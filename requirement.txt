# Core ML / retrieval  (tevatron replaced by local shim in server/tevatron/)
faiss-cpu>=1.7
gensim>=4.2
numpy
scikit-learn
scipy
tokenizers>=0.12
torch>=1.10
tqdm>=4.64
transformers>=4.18

# Hugging Face
huggingface-hub>=0.16

# Web framework (legacy Flask server kept for standalone use)
Flask
flask-cors
waitress

# Gradio UI
gradio>=4.0

# Model download helpers
gdown>=4.7
